<UserControl x:Class="IntegradorView.Pages.InserirModelo.PipelineModelo"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:IntegradorView.Pages.InserirModelo"
        xmlns:usercontrol="clr-namespace:IntegradorView.ControleUsuario"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d" 
        d:DesignHeight="450" d:DesignWidth="800"
        Background="{DynamicResource MaterialDesignPaper}">

    <DockPanel Margin="20,0,20,20">

        <Grid DockPanel.Dock="Bottom">
            <StackPanel Orientation="Horizontal"
                        Margin="0,15,0,0"
                        HorizontalAlignment="Right">

                <Button x:Name="BotaoCancelar" 
                        Content="Cancelar"
                        Style="{StaticResource MaterialDesignOutlinedButton}" 
                        Width="120"
                        Margin="10,0"
                        Command="{Binding NavigateToHomeCommand}"
                        Cursor="Hand"/>

                <Button x:Name="BotaoProximo"
                        Content="Próximo" 
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Width="120"
                        Margin="10,0"
                        Command="{Binding NavigateToTransformersCommand}"
                        Cursor="Hand"/>
            </StackPanel>
        </Grid>


        <Grid Margin="10,10,10,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="7*"/>
                    <RowDefinition Height="4*"/>
                </Grid.RowDefinitions>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    
                    <materialDesign:Card 
                        Grid.Column="0"
                        Width="Auto"
                        Height="Auto"
                        VerticalAlignment="Stretch" 
                        HorizontalAlignment="Stretch" 
                        Margin="0,10,10,10"
                        UniformCornerRadius="10">

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <materialDesign:ColorZone Mode="PrimaryMid" 
                                                      Padding="16" 
                                                      CornerRadius="8,8,0,0"
                                                      Grid.Row="0">
                                <StackPanel Orientation="Horizontal" 
                                            VerticalAlignment="Center">

                                    <materialDesign:PackIcon Kind="Function" 
                                                             Width="24" 
                                                             Height="24" 
                                                             VerticalAlignment="Center" 
                                                             Margin="0,0,10,0"/>

                                    <TextBlock Text="Escolher Função" 
                                    Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                    VerticalAlignment="Center"
                                    Margin="0,0,20,0"/>
                                </StackPanel>

                            </materialDesign:ColorZone>

                            <ScrollViewer VerticalScrollBarVisibility="Auto"
                                          Grid.Row="1">
                                <Grid Margin="16">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                        <ItemsControl ItemsSource="{Binding ListaFeatureEngineering}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel>
                                                        <TextBlock Text="{Binding NomeProcesso}"
                                                                   Grid.Row="1"
                                                                   FontSize="16"
                                                                   Style="{StaticResource MaterialDesignSubtitle1TextBlock}" 
                                                                   Margin="0,15"/>

                                                        <ComboBox Margin="0,0,0,15"
                                                                  Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                                                  Height="Auto"
                                                                  FontSize="14"
                                                                  materialDesign:TextFieldAssist.HasClearButton="True"
                                                                  ItemsSource="{Binding ListaProcessos}"
                                                                  DisplayMemberPath="NomeExibicao"
                                                                  SelectedItem="{Binding FuncaoSelecionada, Mode=TwoWay}"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                </Grid>
                            </ScrollViewer>
                        </Grid>
                    </materialDesign:Card>

                    <materialDesign:Card Grid.Column="1"
                                         Width="325"
                                         Height="Auto"
                                         VerticalAlignment="Stretch" 
                                         HorizontalAlignment="Stretch" 
                                         Margin="10,10,0,10"
                                         UniformCornerRadius="10">

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <materialDesign:ColorZone Mode="PrimaryMid" Padding="16" CornerRadius="8,8,0,0">
                                <TextBlock Text="Pipeline" 
                                        Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                        HorizontalAlignment="Center"
                                        Width="Auto"
                                        Margin="20,0"/>

                            </materialDesign:ColorZone>

                            <Border Grid.Row="2"
                                    BorderThickness="0.5"
                                    Margin="20"
                                    BorderBrush="Black"
                                    CornerRadius="5">

                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                    <ItemsControl ItemsSource="{Binding CardsFuncoes}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <usercontrol:ConfiguracaoCardFuncao/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </Border>

                            

                            <Button Content="Carregar"
                                    Grid.Row="3"
                                    HorizontalAlignment="Right"
                                    Height="40"
                                    Margin="0,0,20,10"
                                    FontSize="14"
                                    FontWeight="Bold"
                                    Style="{StaticResource MaterialDesignRaisedButton}"
                                    Command="{Binding CarregarSchemaCommand}"
                                    Cursor="Hand"/>

                        </Grid>
                    </materialDesign:Card>
                </Grid>
                
                <materialDesign:Card Grid.Row="1"
                                     UniformCornerRadius="10"
                                     HorizontalAlignment="Stretch">
                    
                    <Grid HorizontalAlignment="Stretch">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <materialDesign:ColorZone Mode="PrimaryMid" 
                                                  CornerRadius="8,8,0,0"
                                                  Grid.Row="0"
                                                  HorizontalAlignment="Stretch"
                                                  Width="Auto"
                                                  Height="60"> 
                            <StackPanel Orientation="Horizontal"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center">
                                
                                <materialDesign:PackIcon Kind="Settings" Width="24" Height="24" 
                                                         VerticalAlignment="Center" Margin="0,0,16,0"/>

                                <TextBlock Text="Configurar Função"
                                           Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                          VerticalAlignment="Center"
                                          Margin="0,0,30,0"/>

                                <Button Content="Atualizar"
                                        Command="{Binding AtualizaFuncaoCommand}"
                                        Style="{StaticResource MaterialDesignOutlinedButton}"
                                        Foreground="White"
                                        BorderBrush="White"
                                        Grid.Row="3"
                                        HorizontalAlignment="Right"
                                        Height="40"
                                        Margin="30,10,10,10"
                                        FontSize="14"
                                        FontWeight="Bold"
                                        Cursor="Hand"/>

                                <Button Content="Adicionar"
                                        Command="{Binding AdicionaFuncaoCommand}"
                                        Style="{StaticResource MaterialDesignOutlinedButton}"
                                        Foreground="White"
                                        BorderBrush="White"
                                        Grid.Row="3"
                                        HorizontalAlignment="Right"
                                        Height="40"
                                        Margin="30,10,10,10"
                                        FontSize="14"
                                        FontWeight="Bold"
                                        Cursor="Hand"/>
                            </StackPanel>
                        </materialDesign:ColorZone>

                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20">
                            <ContentControl Content="{Binding TextBox}"/>
                        </ScrollViewer>

                    </Grid>
                </materialDesign:Card>
            </Grid>

            <materialDesign:Card Grid.Column="2"
                                 UniformCornerRadius="10"
                                 Margin="20,10,0,0">

                <GroupBox Header="PREVIEW DOS DADOS"
                          materialDesign:ColorZoneAssist.Mode="PrimaryMid"
                          materialDesign:ColorZoneAssist.Foreground="White">
                    <GroupBox.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" 
                               HorizontalAlignment="Center" 
                               Width="{Binding RelativeSource={RelativeSource AncestorType=ContentPresenter}, Path=ActualWidth}"
                               TextAlignment="Center"
                               FontWeight="Bold"/>
                        </DataTemplate>
                    </GroupBox.HeaderTemplate>

                    <DataGrid x:Name="GridPreview" 
                              ItemsSource="{Binding DataPreview}"
                              AutoGenerateColumns="True" 
                              IsReadOnly="True"
                              CanUserAddRows="False"
                              VerticalScrollBarVisibility="Auto"
                              Style="{StaticResource MaterialDesignDataGrid}">

                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                                <Setter Property="ContentTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}" TextWrapping="Wrap"/>
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                    </DataGrid>
                </GroupBox>
            </materialDesign:Card>
        </Grid>
    </DockPanel>
</UserControl>
