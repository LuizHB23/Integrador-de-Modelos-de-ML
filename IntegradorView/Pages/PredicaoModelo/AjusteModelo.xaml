<Page x:Class="IntegradorView.Pages.PredicaoModelo.AjusteModelo"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:IntegradorView.Pages.PredicaoModelo"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Graficos"
      Background="{DynamicResource MaterialDesignPaper}">

    <Page.Resources>
        <Style x:Key="ModeloCardBotao" TargetType="Button" BasedOn="{StaticResource MaterialDesignOutlinedButton}">
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="MinHeight" Value="300"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="{TemplateBinding BorderThickness}" 
                        CornerRadius="12" Background="{TemplateBinding Background}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="60"/>
                                </Grid.RowDefinitions>

                                <Border Grid.Row="0" CornerRadius="12,12,0,0" ClipToBounds="True">
                                    <ContentPresenter x:Name="ImageContent" ContentSource="Content" />
                                </Border>

                                <Separator Grid.Row="1" Background="{DynamicResource MaterialDesignDivider}" Height="1"/>

                                <Viewbox Grid.Row="2" Margin="10,5">
                                    <TextBlock Text="{TemplateBinding Tag}" FontWeight="Bold" 
                                       Foreground="{DynamicResource MaterialDesignBody}"/>
                                </Viewbox>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#2C3E50" Padding="20">
            <StackPanel>
                <TextBlock Text="CONFIGURAÇÕES ML" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,0,0,30"/>

                <TextBlock Text="Modelo de Inferência" Foreground="#BDC3C7" Margin="0,10,0,5"/>
                <ComboBox Height="30" Margin="0,0,0,20">
                    <ComboBoxItem Content="Regressão Linear"/>
                    <ComboBoxItem Content="Random Forest"/>
                    <ComboBoxItem Content="Rede Neural"/>
                </ComboBox>

                <TextBlock Text="Sensibilidade (Threshold)" Foreground="#BDC3C7" Margin="0,10,0,5"/>
                <Slider Minimum="0" Maximum="1" Value="0.5" TickFrequency="0.1" IsSnapToTickEnabled="True"/>

                <Button Content="Rodar Inferência" Height="40" Margin="0,40,0,0" 
                    Background="#27AE60" Foreground="White" BorderThickness="0"/>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="150"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
                <Button Content="📁 Carregar CSV" Width="120" Height="35"/>
                <TextBlock x:Name="lblStatus" Text="Nenhum arquivo carregado" VerticalAlignment="Center" Margin="15,0,0,0" Foreground="#7F8C8D"/>
            </StackPanel>

            <Border Grid.Row="1" Background="White" CornerRadius="5" Padding="10">
                <DataGrid Name="dgDados" AutoGenerateColumns="True" BorderThickness="0" Background="Transparent"/>
            </Border>

            <UniformGrid Grid.Row="2" Columns="3" Margin="0,20,0,0">
                <Border Background="#E1F5FE" Margin="5" CornerRadius="8">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="Acurácia" HorizontalAlignment="Center" Foreground="#0288D1"/>
                        <TextBlock Text="94.2%" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
                <Border Background="#E8F5E9" Margin="5" CornerRadius="8">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="Tempo Proc." HorizontalAlignment="Center" Foreground="#2E7D32"/>
                        <TextBlock Text="12ms" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
                <Border Background="#FFF3E0" Margin="5" CornerRadius="8">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="Predições" HorizontalAlignment="Center" Foreground="#EF6C00"/>
                        <TextBlock Text="1,240" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </UniformGrid>
        </Grid>
    </Grid>
</Page>
